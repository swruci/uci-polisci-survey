<!-- needed in surveys/new, survey/edit -->
<%= form_with(model: survey, local: true) do |form| %>
  <% if survey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(survey.errors.count, "error") %> prohibited this survey from being saved:</h2>

      <ul>
      <% survey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %><br>
    <%= form.text_field :name, class: 'form-control', :required => true %>
  </div>

  <br>

  <div class="field">
    <%= form.label :description %><br>
    <%= form.text_area :description, class: 'form-control', rows: 4, :required => true %>
  </div>

  <br>

  <div class="field">
    <%= form.label :afterword %><br>
    <%= form.text_area :afterword, class: 'form-control', rows: 4, :required => true %>
  </div>

  <br>

  <div class="actions">
    <%= form.submit class: 'btn btn-secondary' %>
  </div>
<% end %>
